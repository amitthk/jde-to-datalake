// Runtime configuration override
// This allows setting the backend URL at runtime without rebuilding

window.STICAL_CONFIG = {
  BACKEND_URL: 'http://localhost:9998' // Change this to your server IP
};
